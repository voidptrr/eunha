# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/checks.yml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - uses: DeterminateSystems/nix-installer-action@main
    - uses: DeterminateSystems/magic-nix-cache-action@main
    - name: Run checks
      run: nix flake check
name: checks
'on':
  pull_request:
    paths:
    - .github/dependabot.yml
    - .clang-format
    - .clangd
    - .clang-tidy
    - CMakeLists.txt
    - CMakePresets.json
    - scripts/check.sh
    - '**/*.nix'
    - '**/*.c'
    - '**/*.h'
    - '**/CMakeLists.txt'
    - flake.lock
    - .github/workflows/checks.yml
  push:
    branches:
    - main
    paths:
    - .github/dependabot.yml
    - .clang-format
    - .clangd
    - .clang-tidy
    - CMakeLists.txt
    - CMakePresets.json
    - scripts/check.sh
    - '**/*.nix'
    - '**/*.c'
    - '**/*.h'
    - '**/CMakeLists.txt'
    - flake.lock
    - .github/workflows/checks.yml
